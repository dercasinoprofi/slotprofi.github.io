{
  "name": "nuxt-bootstrap-skeleton",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "git rm -rf docs/ && time npm run generate && cp CNAME docs/ && rm -rf docs/images/* && git add docs/ && git commit -am 'production deployment' && git push"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.13.6",
    "core-js": "^3.19.3",
    "mdb-ui-kit": "^3.11.0",
    "nuxt": "^2.15.8",
    "vue": "^2.6.14",
    "vue-server-renderer": "^2.6.14",
    "vue-template-compiler": "^2.6.14",
    "webpack": "^4.46.0"
  },
  "devDependencies": {
    "@nuxt/image": "^0.6.2",
    "@nuxtjs/google-fonts": "^1.3.0"
  }
}
